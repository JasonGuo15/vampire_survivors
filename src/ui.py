import pygame\n\nclass UI:\n    def __init__(self, width, height):\n        self.width = width\n        self.height = height\n        self.health_bar_length = 200\n        self.health_bar_height = 20\n        self.exp_bar_length = 200\n        self.exp_bar_height = 20\n        self.timer_font = pygame.font.Font(None, 36)\n        self.hud_elements = []\n\n    def draw_health_bar(self, surface, health, max_health):\n        health_ratio = health / max_health\n        health_bar_width = self.health_bar_length * health_ratio\n        pygame.draw.rect(surface, (255, 0, 0), (10, 10, self.health_bar_length, self.health_bar_height))  # Background\n        pygame.draw.rect(surface, (0, 255, 0), (10, 10, health_bar_width, self.health_bar_height))  # Current health\n\n    def draw_exp_bar(self, surface, exp, max_exp):\n        exp_ratio = exp / max_exp\n        exp_bar_width = self.exp_bar_length * exp_ratio\n        pygame.draw.rect(surface, (255, 255, 0), (10, 40, self.exp_bar_length, self.exp_bar_height))  # Background\n        pygame.draw.rect(surface, (0, 0, 255), (10, 40, exp_bar_width, self.exp_bar_height))  # Current experience\n\n    def draw_timer(self, surface, time_left):\n        timer_text = self.timer_font.render(str(time_left), True, (255, 255, 255))\n        surface.blit(timer_text, (10, 70))\n\n    def draw(self, surface, health, max_health, exp, max_exp, time_left):\n        self.draw_health_bar(surface, health, max_health)\n        self.draw_exp_bar(surface, exp, max_exp)\n        self.draw_timer(surface, time_left)\n        for element in self.hud_elements:\n            element.draw(surface)\n\n